# keybridgeBT mac-sender configuration
listen_host: "0.0.0.0"      # bind address ("0.0.0.0" = all interfaces)
listen_port: 9741            # TCP port to listen on
hotkey_keycode: 40           # macOS virtual keycode for K
hotkey_modifiers: 0x0C0000   # Ctrl+Option
log_level: INFO
